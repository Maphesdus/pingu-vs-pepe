using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Represents a generic gun
/// </summary>
/// We want a few different types of guns, so this class will mostly have common variables and methods
public class Gun : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    protected void fireBullet(Vector3 direction, float bulletDamage) {

    }
}
